# Extra packages for running the ML4HEP notebooks in Google Colab
# (the fallback when BinderHub is unavailable).
#
# Colab already ships torch, scipy, matplotlib, h5py and scikit-learn; this adds
# what's missing. Install with one line:
#
#     !pip install -r requirements-colab.txt
#
# numpy is pinned <2 because jetnet (energyflow/wasserstein) needs it — pip will
# downgrade Colab's numpy 2.x, which is expected. Restart the runtime afterwards.
# The --extra-index-url lets pip find cadence-edu on TestPyPI; everything else
# resolves from regular PyPI.
--extra-index-url https://test.pypi.org/simple/
numpy<2
diffusers
jetnet
cadence-edu==0.3.0
