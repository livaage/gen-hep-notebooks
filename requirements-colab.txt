# Extra packages for running the ML4HEP notebooks in Google Colab
# (the fallback when BinderHub is unavailable).
#
# Colab already ships torch, numpy, scipy, matplotlib, h5py and scikit-learn, so
# this only adds what's missing. It keeps Colab's numpy 2.x, so NO restart needed.
#
#     !pip install -r requirements-colab.txt
#
# NOTE: jetnet is deliberately NOT here. It pins numpy<2 (via energyflow ->
# wasserstein), which forces a one-time kernel restart. Only notebook 06 needs the
# real JetNet dataset, so it installs jetnet in its own cell; notebooks 01/02/05
# fall back to synthetic jets on Colab (they use real jets on BinderHub).
--extra-index-url https://test.pypi.org/simple/
diffusers
cadence-edu==0.3.0
